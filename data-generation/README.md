# Data Generation

Generates 8 interconnected CSV files with realistic fintech data into `raw table output/`.

**Commands:** `deno task generate` (full) | `deno task sample` (small) | `deno task targets` (targets.csv)

**Customize:** Edit `generate.ts` for `customerCount`, `startDate`/`endDate`, or change models in `models/fintech/`.
